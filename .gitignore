# --- OS junk ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Editors / IDEs ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- Python (helper/check scripts) ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# --- OpenSCAD ---
# Backup files OpenSCAD writes next to the .scad you're editing
*.scad~
*.scad.bak
# Auto-saved/incomplete exports
*.stl.tmp
*.off.tmp
# OpenSCAD editor autosave/session files
.openscad_autosave

# --- FreeCAD ---
*.FCBak
*.FCStd1
*.FCStd.lock

# --- Blender ---
*.blend1
*.blend2
*.blend[0-9]+

# --- CAD/mesh caches & intermediate exports ---
# Uncomment if you'd rather regenerate these from source (.scad/.py) than track them:
# *.stl
# *.off
# *.amf
# *.3mf
# *.png
# *.csg

# --- Slicer output (if you ever export g-code alongside models) ---
*.gcode

# --- Logs / misc temp ---
*.log
*.tmp
.cache/

# --- generated trace intermediates (see coasters/trace_*.py) ---
# Each trace script deletes its own in a finally block, so these should
# never survive a run -- including an aborted or --check one. Listed anyway
# for the run that gets killed between writing and cleaning up.
.tree_trace.dxf
.tree_trace.scad
.triskele_trace.dxf
.triskele_trace.scad
